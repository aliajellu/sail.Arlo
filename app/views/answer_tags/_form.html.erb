<%= form_for(@answer_tag) do |f| %>
  <% if @answer_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer_tag.errors.count, "error") %> prohibited this answer_tag from being saved:</h2>

      <ul>
      <% @answer_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer_id %><br />
    <%= f.text_field :answer_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br />
    <%= f.text_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
